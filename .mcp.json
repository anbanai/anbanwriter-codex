{
  "mcpServers": {
    "anban": {
      "type": "http",
      "url": "${ANBAN_API_URL:-https://api.creator.anbanai.com}/mcp",
      "headers": {
        "Authorization": "Bearer ${ANBAN_API_KEY}"
      },
      "timeout": 900000
    }
  }
}
